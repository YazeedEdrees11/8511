# Copy to .env.docker and fill in real values. .env.docker is gitignored.

# MSSQL
MSSQL_SA_PASSWORD=ChangeMe_StrongPassword123!

# App: when running via docker-compose, the DB host is the service name "mssql"
DATABASE_URL=sqlserver://mssql:1433;database=eighty_five_eleven;user=sa;password=ChangeMe_StrongPassword123!;encrypt=true;trustServerCertificate=true

# RAG / embeddings
GEMINI_API_KEY=
HUGGINGFACE_API_KEY=
